import styled from 'styled-components'

const Content = styled.div`
  color: var(--color-alt);
  font-size: var(--fs-lge);
`
const Box4Content = <Content>Box4 Content</Content>

export default Box4Content
